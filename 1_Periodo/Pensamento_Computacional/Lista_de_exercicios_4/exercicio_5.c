#include <stdio.h>

main() {
    // Declarar variaveis
    // Armazenar idade, sexo, salario base, ano contratação e matricula
    // Se idade >= 18
        // Se M, 10% de reajuste
        // Se F, 8% de reajuste
    // Senão se idade >= 40
        // Se M, 8% de reajuste
        // Se F, 10% de reajuste
    // Senão se idade >= 70
        // Se M, 15% de reajuste
        // Se F, 17% de reajuste
}